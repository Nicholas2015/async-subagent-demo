[project]
name = "async-subagent-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Nicholas Sun", email = "suncy_software@hotmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "deepagents",
    "langgraph",
    "langgraph-sdk",
    "langchain-openai",
    "langgraph-cli[inmem]",
    "python-dotenv>=1.2.3",
]

[tool.uv]
package = false

[project.scripts]
async-subagent-demo = "async_subagent_demo:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
